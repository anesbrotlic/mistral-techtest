export class AppConstants{

    static jwt_local_storage_key="jwt";
    static movie_rated_success="Movie rated successfully!";

    static rating_search_type_keywords=['stars','star','rating'];
    static rating_keyword_type_all_keywords=['more than','less than','at least','at most'];
    
    static rating_more_kw=['more than'];
    static rating_less_kw=['less than'];
    static rating_at_least_kw=['at least'];
    static rating_at_most_kw=['at most'];
    
    static release_search_type_keywords=['years','year','after','before', 'at least', 'at most','older than'];
    static release_keyword_type_all_keywords=['after','before', 'at least', 'at most','older than'];

    static release_after_kw=['after'];
    static release_before_kw=['before'];
    static release_older_kw=['older than'];
    static release_at_least_kw=['at least'];
    static release_at_most_kw=['at most'];

}